<h1>Rides#index</h1>
<p>Find me in app/views/rides/index.html.erb</p>

<% @rides.each do |ride| %>
  <%= ride.title %>

<%= ride.price %>
<%= ride.starting_point %>
<%= ride.destination %>
<%= ride.seats %>
<% end %>
